{% extends 'base.html.twig' %}

{% block title %}New Album{% endblock %}

{% block body %}

    <div class="container mt-5 pt-5">

        
        <h1>Créer un Nouvel album photos</h1>

        <a href="{{ path('album_index') }}" class="btn btn-info my-3">
            <i class="fas fa-arrow-circle-left mr-2"></i>Retour à la liste</a>
        
        {{ include('album/_form.html.twig') }}

        {# {% for pic in album.images %}
            <p>{{pic.name}}</p>
            
        {% endfor %} #}
        
    </div>
{% endblock %}
